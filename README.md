WFM Median Repricer 
---
Fetches your active sell orders from warframe.market,
computes the 48-hour median sell price for each item,
and updates prices automatically — asking you first
whenever the new price would be LOWER than the current one.

Items with fewer than MIN_TRADES_48H sales in the past 48 h
are never touched and shown in a summary list at the end.

Setup
---
1.  download wfm_repricer.py and put it wherever you want
1.  pip install curl_cffi keyring   (tkinter ships with Python on Windows)
2.  Get your JWT token:
      - Log into warframe.market in your browser
      - Open DevTools (F12) → Application/Storage → Cookies → warframe.market
      - Copy the value of the "JWT" cookie
3.  Run:  python wfm_repricer.py
    You'll be prompted for the token on first run; it's saved to the (windows) credential manager.

Disclaimer: This was basically entirely generated by AI and only edited by me when things broke.
            It also should not be trusted with anything other than simple items as my testing has been 
            running it once and seeing that it didn't throw an error. I'm aware that the median price for anything
            leveled is garbage, but that is, as of now, a limitation of warframe.market's statistics.

Might come back to this later and clean it up / add some functionality
